{
  "rules": {
    ".read": false,
    ".write": false,

    "games": {
      "$gameId": {
        "players": {
          "$uid": {
            ".read": "auth != null",
            ".write": "auth != null && auth.uid === $uid",
            "joinedAt":   { ".validate": "newData.isNumber()" },
            "lastActive": { ".validate": "newData.isNumber()" }
          }
        }
      }
    }
  }
}
