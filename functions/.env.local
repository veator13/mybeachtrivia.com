# Spotify API Credentials
# Replace these placeholder values with your actual credentials from Spotify Developer Dashboard
SPOTIFY_CLIENT_ID=your_spotify_client_id_here
SPOTIFY_CLIENT_SECRET=your_spotify_client_secret_here

# Redirect URI - Must match exactly what's registered in your Spotify Developer Dashboard
# For production (must be added to your Spotify Dashboard)
SPOTIFY_REDIRECT_URI=https://beach-trivia-website.web.app/spotifyCallback

# For local development (must also be added to your Spotify Dashboard)
SPOTIFY_REDIRECT_URI_DEV=http://localhost:5050/spotifyCallback

# Firebase project settings
FIREBASE_PROJECT_ID=beach-trivia-website
FIREBASE_STORAGE_BUCKET=beach-trivia-website.appspot.com

# Other application settings
NODE_ENV=development

# IMPORTANT NOTES:
# 1. For Firebase Cloud Functions in production, you should set these using Firebase Config instead:
#    firebase functions:config:set spotify.client_id="YOUR_ID" spotify.client_secret="YOUR_SECRET"
#
# 2. This .env.local file is primarily for local development with Firebase Emulators
#
# 3. Remember to add .env.local to your .gitignore file to avoid committing secrets